spring:
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/qawave
    username: qawave
    password: qawave_dev_password

  flyway:
    enabled: true
    url: jdbc:postgresql://localhost:5432/qawave
    user: qawave
    password: qawave_dev_password
    locations: classpath:db/migration

  data:
    redis:
      host: localhost
      port: 6379

  kafka:
    bootstrap-servers: localhost:9094

logging:
  level:
    com.qawave: DEBUG
    org.springframework.r2dbc: DEBUG
